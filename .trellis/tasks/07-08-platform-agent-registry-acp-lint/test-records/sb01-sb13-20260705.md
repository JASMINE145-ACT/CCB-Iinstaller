# Test Records — 07-08 Task A (SB-01 + SB-13)

**Date:** 2026-07-05

## SB-01 — Registry-driven agent fleet

| Check | Result |
|-------|--------|
| `packageRegistry.test.ts` (monorepo overlay) | 4/4 PASS |
| `agentSessionProfile` registry parity test | PASS (`CCB_DEFAULT_SESSION_AGENT_ID === wande-orchestrator`) |
| `build-package-registry.mjs` | 0 errors |

## SB-13 — Forbidden-coupling lint

| Check | Result |
|-------|--------|
| `lint-platform-forbidden-coupling.mjs` | PASS — 50 known hits, 0 new |
| Baseline | `ccb-installer/config/lint/platform-forbidden-coupling.baseline.json` |
| CI | `.github/workflows/forbidden-coupling.yml` |

## Notes

- `agentSessionProfile.test.ts` orchestrator/L1 cases may fail locally when `employee-profile.json` is present on disk (pre-existing P9 env); not introduced by SB-01.
- Deploy: run `sync-claude-code-b-mcp-prefetch.ps1 -Build -Deploy` before live ACP smoke.
