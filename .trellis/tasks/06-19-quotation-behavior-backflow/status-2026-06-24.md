# Status — 2026-06-24

> **Superseded by [`status-2026-06-29.md`](./status-2026-06-29.md).** The `useAcpMessage` consumer was never implemented; task re-opened P0.

## Renderer fix landed (aionui-src) — partial

**Problem:** Idle-killed quotation sessions still replay prior-turn assistant blocks on the next send (数据倒灌).

**Layer:** AionUI renderer — explicit post-idle wake window, not aioncore.

### Implementation

| File | Change |
|------|--------|
| `packages/desktop/src/renderer/pages/conversation/runtime/postIdleWakeWindow.ts` | New module: per-conversation wake window state |
| `packages/desktop/src/renderer/pages/conversation/platforms/acp/AcpSendBox.tsx` | `beginPostIdleWakeWindow` before forced warmup; `acceptPostIdleWakeTurn` after `markSendAccepted`; `clearPostIdleWakeWindow` on error |
| `packages/desktop/src/renderer/pages/conversation/platforms/acp/useAcpMessage.ts` | `shouldDropStaleTurnMessage` prioritizes wake window over runtime snapshot; clears window when runtime completes |

### Lifecycle

```
Send click
  → beginPostIdleWakeWindow()     [drop ALL turn-scoped WS]
  → prepareRuntimeSync({ force: true })
  → sendMessage → turn_id
  → acceptPostIdleWakeTurn(turn_id) [only this turn_id for 10s]
  → stream to UI
  → clear on fail / runtime idle / reset
```

### Verification (local, aionui-src)

- oxlint: 4 related files — 0 warnings / 0 errors
- vitest: `postIdleWakeWindow.test.ts` — 4 passed
- `git diff --check` — pass

### Not yet done

- [ ] Rebuild AionUI + CCB-Wanding installer (current `CCB-Wanding-1.1.1.exe` is old renderer)
- [ ] User smoke: same conversation idle ≥61min → follow-up without duplicate blocks
- [ ] aioncore `ReplaySuppressionGuard` (2026-06-19) remains complementary; this fix closes the renderer race independent of backend replay suppression

### Spec cross-ref

`.trellis/spec/frontend/chat-acp-flow.md` — §2026-06-24 post-idle wake window
