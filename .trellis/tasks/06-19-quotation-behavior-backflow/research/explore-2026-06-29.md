# Replay backflow — explore notes (2026-06-29)

## User report (via 06-29 session context)

- Agent killed (idle timeout)
- **Before or when sending new query:** entire previous assistant response gets stuffed together with the new reply
- 06-29 profile-drift fixes did **not** resolve this

## Code audit summary

### Half-wired fix (06-24)

`getPostIdleWakeWindowTurnId` is only referenced in:

- `postIdleWakeWindow.ts` (definition)
- `postIdleWakeWindow.test.ts`
- `AcpSendBox.tsx` (begin/accept/clear only)

**Not referenced in `useAcpMessage.ts`.**

### Replay source (CCB)

`ccb-installer/src/services/acp/agent.ts`:

- `getOrCreateSession` / `loadSession` loads disk messages
- Calls `replayHistoryMessages` → `conn.sessionUpdate({ sessionUpdate: 'agent_message_chunk', ... })`

Idle kill comment in same file:

> aioncore may keep a persisted session id after IdleTimeout kills the CLI

### Merge sink (AionUI)

`composeMessage` / `composeMessageWithIndex`:

- `last.msg_id === message.msg_id` → `existing.content + incoming.content`
- Explains concatenation into one bubble when replay reuses `msg_id`

### False documentation

| Doc | Claim | Reality |
|-----|-------|---------|
| `status-2026-06-24.md` | `useAcpMessage` has `shouldDropStaleTurnMessage` | Symbol absent |
| `runtime-audit-2026-06-18.md` | `useAcpMessage` rejects stale turn-scoped events | Not in current source |
| `chat-acp-flow.md` | §2026-06-24 post-idle wake window | Section never added |

### Active debug instrumentation

`AcpSendBox.tsx` imports `debugSessionLog` (session `c39a5e`, comment: remove after replay bug verified fixed).

## Repro checklist (operator)

- [ ] Idle ≥ `acp.agentIdleTimeout` (default 5min) or kill aioncore child
- [ ] Reopen same quotation conversation **without sending** — note spurious assistant growth
- [ ] Send follow-up — note old+new in one bubble
- [ ] DevTools: WS `text`/`content` events between `beginPostIdleWakeWindow` and `acceptPostIdleWakeTurn`
- [ ] Compare `msg_id` / `turn_id` on stale vs new events
- [ ] CCB log: `replayHistoryMessages` / `stale session rehydrated`

## Related tasks

- `06-29-specialist-session-resume-profile-drift` — orthogonal (profile only)
- `06-29-acp-prompt-orphan-cleanup-on-query-next-timeout` — duplicate prompt on stuck drain (different symptom)
