# Status — 2026-06-29 (re-opened)

**Task:** [`prd.md`](./prd.md)  
**Previous:** [`status-2026-06-24.md`](./status-2026-06-24.md) — claimed renderer fix landed  
**Explore:** [`research/explore-2026-06-29.md`](./research/explore-2026-06-29.md)

## Verdict: 06-24 fix **incomplete** — task stays open

User repro (2026-06-29): after agent kill, prior assistant reply still merges with new query response. Profile-drift work (06-29) does not address this.

### What actually landed (aionui-src)

| File | State |
|------|-------|
| `runtime/postIdleWakeWindow.ts` | ✅ Module + unit tests (4/4) |
| `platforms/acp/AcpSendBox.tsx` | ✅ begin / accept / clear on send path |
| `platforms/acp/useAcpMessage.ts` | ❌ **No consumer** — `shouldDropStaleTurnMessage` never implemented |
| `common/chat/chatLib.ts` | ❌ `transformMessage` does not set `turn_id` on `TMessage` |

### Root cause (confirmed)

```
warmup / loadSession
  → CCB replayHistoryMessages (assistant chunks on WS)
  → useAcpMessage (no stale filter)
  → composeMessage merges by msg_id
  → old + new text in one bubble
```

Wake window opens in `AcpSendBox` but **nothing reads** `getPostIdleWakeWindowTurnId`.

### Next implementation (Phase A–B)

1. Implement `shouldDropStaleTurnMessage` in `useAcpMessage.handleResponseMessage`
2. Pass `turn_id` in `transformMessage`
3. Add `useAcpMessage.dom.test.ts` cases for wake-window drop + accept
4. Phase C: reopen auto-warmup guard
5. Write `chat-acp-flow.md` § post-idle wake window

### Verification target

```text
cd D:\Projects\aionui-src
bunx vitest run tests/unit/conversation/runtime/postIdleWakeWindow.test.ts
bunx vitest run tests/unit/renderer/useAcpMessage.dom.test.ts --pool=threads
```

Manual: idle 6min → reopen (no duplicate pre-send) → send follow-up (clean new bubble).

### Not done (unchanged from 06-24)

- [ ] Renderer consumer wired
- [ ] Rebuild / installer smoke
- [ ] Spec section in chat-acp-flow.md
