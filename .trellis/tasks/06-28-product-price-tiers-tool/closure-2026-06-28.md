# Closure — product price tiers tool (2026-06-28)

## Exploration answer

**Before this task, the agent did NOT have the workflow you described.**

| Capability | Before | After |
|------------|--------|-------|
| Single default price | `match_quotation` → one `unit_price` for `customer_level` | unchanged |
| All price tiers for one SKU | ❌ | ✅ `get_product_price_tiers` |
| Explain tier names via `data.Md` Read | Agent told **not** to Read `data.Md` daily | Tool returns `tier_guide_summary` + Chinese labels |
| `get_profit_by_price` + `all_levels` | Exists in `inventory_agent_tools` only; needs `price` input; not on quotation MCP | Still deferred for profit lookup |

## Delivered

| Item | Path |
|------|------|
| Core | `python/quotation/price_tiers.py` |
| Dispatch | `python/quotation/price_tiers_dispatch.py` |
| MCP | `mcp_servers/quotation-server/dist/index.js` |
| Agent | `ccb-installer/config/agents/quotation-agent.md` §价格档位查询 |
| Tests | `python/tests/test_price_tiers.py` |

## Verification

| Check | Result |
|-------|--------|
| Code review | **PASS** (53bee38b) |
| Unit tests | **5/5 OK** (vendor python) |
| Vendor sync | `sync-dev-wanding-vendor.ps1` |
| Agent deploy | `deploy-seed-agents.ps1 -ForceMd` |

## Employee conversation flow

```
用户：10012005* 青山价和 B 价多少？有哪些价格种类？

1. get_product_price_tiers { "code": "10012005*" }
2. 回复 tiers 表格（price_d / price_b / factory_inc_tax …）
3. 用 tier_guide_summary 解释档位含义 — 不必 Read data.Md
```

若只有描述没有 code → 先 `match_quotation` 拿 code → 再 `get_product_price_tiers`。

**Task status:** `completed` (2026-06-28)
