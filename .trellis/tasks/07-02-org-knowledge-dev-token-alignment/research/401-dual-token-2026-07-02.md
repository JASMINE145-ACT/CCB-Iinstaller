# Research — org knowledge 401 dual-token (2026-07-02)

## Symptom

`append_business_rule` → `org knowledge doc not available: wanding_business_knowledge`

Log: `org knowledge API GET /api/org-knowledge/wanding_business_knowledge failed: HTTP Error 401: Unauthorized`

## Evidence (this machine)

| Token source | HTTP GET `/api/org-knowledge/wanding_business_knowledge` |
|--------------|----------------------------------------------------------|
| No Authorization | 401 |
| `%APPDATA%/AionUi/aionui/org-session.token` (Prod) | **401** |
| `%APPDATA%/AionUi-Dev/aionui/org-session.token` (Dev) | **200** (version=3) |

MCP config (`D:\CCB-Wanding\ccb-mcp.json`):

```json
"ORG_SESSION_TOKEN_FILE": "C:\\Users\\m1774\\AppData\\Roaming\\AionUi\\aionui\\org-session.token"
```

Both token files exist; contents differ (len=271 each).

## Code gap (initial)

| Module | Multi-token candidates | 401 retry |
|--------|---------------------|-----------|
| `org_price_client.py` | Yes (2026-06-29) | Yes |
| `org_knowledge_client.py` | No (first token only) | No |

`Get-OrgSessionTokenFilePath` in `ensure-wanding-settings.ps1` returned first **existing** path (Prod before Dev), without validity check.

## Why read path still works

`load_doc_content()` → `get_doc()` fails → falls back to `WANDING_BUSINESS_KNOWLEDGE_PATH` shadow md.

`append_business_rule()` → `get_doc(use_cache=False)` → **no fallback** → RuntimeError.

## User decision

2026-07-02: 新开 task，长久解决。v1 提议对齐 price 401 盲扫；**v2 评审**升级为 profile-strict `org_session.py`（见 PRD）。

---

## Resolution (v2 — recorded in spec 2026-07-02)

| Layer | Change |
|-------|--------|
| `python/admin/org_session.py` | Shared profile + JWT candidates; STRICT / LEGACY_SCAN |
| `org_knowledge_client.py` | Multi-candidate GET/PUT; 401/403/409 typed errors |
| `org_price_client.py` | Migrated to `org_session` |
| `ensure-wanding-settings.ps1` | `AIONUI_APPDATA_PROFILE` + profile-scoped `ORG_SESSION_TOKEN_FILE` |
| `start-dev-full.ps1` | `$env:AIONUI_APPDATA_PROFILE = 'AionUi-Dev'` |

**Spec:** [`.trellis/spec/integration/org-knowledge.md`](../../../spec/integration/org-knowledge.md) § MCP `org_session` profile contract; [`price-library.md`](../../../spec/integration/price-library.md) § Org JWT; [`dev-sync-playbook.md`](../../../spec/integration/dev-sync-playbook.md) §4.6.

**Verify (this machine):**

- Unit: `admin.test_org_session` + `admin.test_org_knowledge_client` + `tests.test_org_price_client` — 39/39 OK
- Vendor smoke: `sync-dev-wanding-vendor -UpdateSettings -Smoke` — PASS
- Live GET with Dev profile: `get_doc('wanding_business_knowledge')` → `org-api version 3`
- `ccb-mcp.json`: `AIONUI_APPDATA_PROFILE=AionUi-Dev`, token path → `AionUi-Dev\...`

**Open:** manual `append_business_rule` e2e (new Guid session after restart).

**Same-day unrelated incident:** send blocked by `Route not found.` (07-01, aioncore `/mode` vs `config-options`) — not org JWT; see [`07-01/.../route-not-found-config-options-2026-07-02.md`](../07-01-aionui-full-auto-permission-sync/research/route-not-found-config-options-2026-07-02.md).
