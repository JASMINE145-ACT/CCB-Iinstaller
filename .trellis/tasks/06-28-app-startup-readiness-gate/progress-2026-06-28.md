# App Startup Readiness Gate — progress 2026-06-28

**Task:** `.trellis/tasks/06-28-app-startup-readiness-gate/`  
**Status:** in_progress (MVP Layer 1+2)

## Implemented

| Component | Path | Notes |
|-----------|------|-------|
| MCP warm script | `ccb-installer/lib/warm-wanding-mcp.mjs` | quotation + accurate stdio warm (mirrors `wanDMcpWarmup.ts`) |
| Main pipeline | `aionui-src/.../ccbStartupReadiness.ts` | Layer 1 health (no probe) + Layer 2 warm; 120s timeout → soft_ready |
| Shared types | `ccbStartupReadinessShared.ts` | renderer-safe `isCcbStartupSendAllowed` |
| IPC | `ccbMcpBridge.ts` + `ccbIpcBridge.ts` | `getStartupReadiness`, `ensureStartupReadiness`; auto-start on init |
| Hook | `useCcbStartupReadiness.ts` | poll until ready |
| Guid UI | `CcbStartupReadinessBanner.tsx`, `GuidPage.tsx` | banner + send disabled until ready |
| Send gate | `useGuidSend.ts`, `AcpSendBox.tsx`, `useAcpInitialMessage.ts` | await startup readiness before send/warmup |
| Unit test | `ccbStartupReadinessShared.test.ts` | 4/4 pass |

## Dev verify (manual)

1. Restart AionUI dev (`start-dev-full.ps1 -SkipBootstrap`)
2. Login → Guid page should show「正在检查配置…」→「正在预热报价 MCP…」
3. Send disabled until ready; then「查询 直接50 价格」should not `Failed to fetch`
4. Logs (main): MCP warm starts **before** any conversation create

## Init crash (fixed 2026-06-28)

`initCcbMcpBridge` called `isCcbMcpAuthorityActive().then(...)` but the function returns a **sync boolean** → main process exit 255 (`.then is not a function`). Fixed to synchronous `if (isCcbMcpAuthorityActive()) startCcbStartupReadinessPipeline()`.

## Spec recorded

- [`mcp-health.md`](../../spec/integration/mcp-health.md) § App startup readiness gate
- [`agents-unified-model.md`](../../spec/integration/agents-unified-model.md) cold start + Failed to fetch rows
- [`build-deploy-verify.md`](../../spec/backend/build-deploy-verify.md) § 3.4a
- [`internal-update.md`](../../spec/integration/internal-update.md) #23
- [`wanding-packaging-whitelist.md`](../../spec/integration/wanding-packaging-whitelist.md) § 8.4

## Not in MVP

- Layer 3 anchor ACP session
- Move `scheduleWanDMcpWarmup` off session/new (still runs; app warm reduces impact)
- NSIS/hot zip packaging of `lib/warm-wanding-mcp.mjs` to `D:\CCB-Wanding\lib`

## Test evidence

```
bun test tests/unit/common-config/ccbStartupReadinessShared.test.ts → 4 pass
```
