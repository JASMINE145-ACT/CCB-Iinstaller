# Pre-flight benefit review — 已规划改动的预期收益（2026-07-15）

在动 D1 之前复审。结论：**先做 D1 矩阵收益最高、风险最低**；零卡 UI 与架构大改的收益依赖 D，现在写代码不划算。

## 按阶段

| Phase | 改什么 | 预期收益 | 代价 / 风险 | 现在做？ |
|-------|--------|----------|-------------|----------|
| **D1 矩阵+oracle** | 文档：锁提示词、工具、成功字段、计时 | 把「调了 Agent」与「业务做完」拆开；后续修漏洞有靶心；避免改 prompt 碰运气 | 几乎零运行时风险 | **是（本轮）** |
| **D2a/b 修红** | Case1 超时 / Case3 L0 bleed | 主入口真正能交结果；零卡才有底气 | 动 L1/session 注入，需 TDD | D1 锁完后再批 |
| **D3 跑绿+计时** | 证据与 latency 三截 | 可宣称「可默认单入口」的硬证明 | 测评时间（分钟～小时级） | D2 后 |
| **G 零卡** | Guid 列表空 | 认知负担↓；「只会主入口」产品形态 | 失去卡面逃生舱；价库靠侧栏必须验 | **仅 D PASS 后** |
| **架构大改主 agent** | （未开） | 不确定；可能改善路由，也可能伤稳路径 | 高 | **不做**，等矩阵证明 |
| **pi / lazy MCP 再收紧** | docs-only | 几乎无新增产品收益（已有 excel/exa defer） | 误 defer 报价/账务会伤冷启 | **不做** |

## 净收益判断（对本机 WanD）

1. **最大收益不在换引擎，在证明 Path A 能交付。** 零卡若建在未证明的委派上 = 拆逃生舱。  
2. **D1 本身不改变用户体感**，但把失败定义钉死（字段/工具/超时），是后面所有修的「杆」。  
3. **零卡收益真实但后置**：简单好看；价库走 `#/price-library` 已够用，前提是员工知道侧栏。  
4. **不在本批做架构手术**的理由：无对照矩阵时改 L1，无法区分「改好了」还是「换了一种失败」。

## 本轮执行范围（遵守 plan 门禁）

- ✅ **D1**：填满并锁定 `mvp-matrix.md`（环境钉 + 行 oracle + eval 映射）  
- ❌ 不改 Guid filter / 不改 orchestrator / 不动 MCP lazy  

若 D1 签字后用户说继续，下一刀是 **D2a（timeout）与 D2b（L0 bleed）分治**，不是零卡。
