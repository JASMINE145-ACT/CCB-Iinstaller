# Closure — 06-28-price-tiers-data-md-read-hook

**Date:** 2026-06-28  
**Status:** completed

## Problem

`get_product_price_tiers` shipped with static `tier_guide_summary` (青山→price_d, 默认→price_b) — wrong for RUCIKA, CEILING, 国标管件, PE where the same field names mean different things per `data.Md` §来源映射.

## Solution

**Prompt hook (same pattern as business knowledge):**

- Call `get_product_price_tiers` → agent **must Read** `D:\CCB-Wanding\vendor\wanding\data\data.Md` same turn
- Tool returns numbers + `product_type` / `source_sheet` / `data_md_path`
- Agent explains labels using §来源映射 for that product type

## Changes

| File | Change |
|------|--------|
| `ccb-installer/config/agents/quotation-agent.md` | New §价格字段契约 data.Md; Read hook; RUCIKA/国标管件 examples |
| `python/quotation/price_tiers.py` | Neutral labels; `data_md_path`; local fallback metadata + neutral labels |
| `mcp_servers/quotation-server/dist/index.js` | Tool description requires Read data.Md |
| `python/tests/test_price_tiers.py` | Assert `data_md_path`, no 青山 in summary |
| `.trellis/spec/integration/agents-unified-model.md` | data.Md Read on multi-tier |
| `.trellis/spec/integration/price-library.md` | § Multi-tier query + data.Md read hook |
| `.trellis/spec/integration/index.md` | Decision-tree row |

## Verification

- Code review: **PASS** (after local-fallback label fix)
- Tests: `python -m unittest tests.test_price_tiers` → **4/4 OK**
- Deploy: `sync-dev-wanding-vendor.ps1 -RepoRoot ...` + `deploy-seed-agents.ps1 -ForceMd`

## Operator E2E

1. Restart dev + **new** quotation conversation
2. Ask: `10012005* 有哪些价格种类？`
3. Expect: Read data.Md + get_product_price_tiers; RUCIKA semantics (第一组/第二组), not 青山

## Deferred

- Stop/PostToolUse enforcement hook for data.Md Read (SOP-only today)
- `ccb-wanding-quotation.md` maint source sync
