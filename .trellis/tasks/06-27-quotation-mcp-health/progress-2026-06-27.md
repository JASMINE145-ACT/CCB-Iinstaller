# Quotation MCP Health (#20) — 2026-06-27

## Problem

Guid 万鼎报价专家调用 `match_quotation` / `get_inventory_by_code` 失败：

```text
Python produced no JSON output
python.exe: can't open file '...\claude-temp-*\python\main.py': No such file or directory
```

`test-mcp-health.ps1`（无 `-Probe`）22/22 PASS，但未检查 `vendor/wanding/python/main.py`、未验证 `CCB_PROJECT_ROOT`、未做 Python 工具实调。

## Changes

| Area | File | Change |
|------|------|--------|
| Runtime | `mcp_servers/quotation-server/dist/config.js` | `resolveProjectRoot()`: honor `CCB_PROJECT_ROOT` only if `python/main.py` exists; fallback `CCB_INSTALL_DIR/vendor/wanding` or dist-relative `../../../wanding` |
| Manifest | `ccb-installer/config/mcp-health-manifest.json` | `vendor/wanding/python/main.py`; `probe_tool_call.match_quotation` |
| Probe lib | `ccb-installer/lib/mcp-stdio-probe.mjs` | Optional `tools/call` after `tools/list`; detect missing-main.py errors |
| CLI probe | `ccb-installer/scripts/test-mcp-probe-layer.mjs` | Pass `probe_tool_call`; `--server=` filter |
| CLI health | `ccb-installer/scripts/test-mcp-health.ps1` | Config check `quotation.env.CCB_PROJECT_ROOT` → `python/main.py` |
| AionUI | `ccbMcpHealthManifest.ts`, `ccbMcpHealth.ts`, test | Mirror manifest; config check; 完整探测 `quotation:python` via shared probe script |

## Verification (dev machine `D:\CCB-Wanding`)

```powershell
.\ccb-installer\scripts\test-mcp-health.ps1           # 24/24 PASS
.\ccb-installer\scripts\test-mcp-health.ps1 -Probe    # 4/4 PASS; quotation tool_call=match_quotation
.\ccb-installer\scripts\test-mcp-health.ps1 -Probe -Session  # 5/5 ACP profiles PASS
cd D:\Projects\aionui-src; bun test tests/unit/common-config/ccbMcpHealth.test.ts  # 1/1 PASS
```

Code-review agent: **PASS** (2026-06-27).

## Update 2026-06-28 — AOL inventory credentials

**Symptom:** Guid 库存状态 — `AOL_ACCESS_TOKEN / AOL_SIGNATURE_SECRET / AOL_DATABASE_ID` 未配置.

**Root cause:** `python/main.py` loads `vendor/wanding/.env.accurate` but install never wrote it; MCP env-only path could leave Python with empty `AOL_*`.

**Fix:** `ensure-wanding-settings.ps1` writes `.env.accurate`; health manifest + probe `get_inventory_by_code`; `aol_configured()` checks all three vars.

**Verify:** config 29/29 · probe `match_quotation+get_inventory_by_code` · session 5/5 · pytest inventory 6/6.

## Spec anchors (#20)

| Doc | Section |
|-----|---------|
| `integration/mcp-health.md` | §2026-06-27 Python path · §2026-06-28 AOL · § MCP+Skill coverage |
| `integration/internal-update.md` | §12.9 row #20, §12.9.3 steps 2b–2c |
| `integration/wanding-packaging-whitelist.md` | §8.3, §12, §16.6 |
| `integration/agents-unified-model.md` | Wrong Python path + inventory AOL rows |
| `backend/config-layer.md` | `CCB_PROJECT_ROOT`, `AOL_*`, `.env.accurate` |
| `backend/build-deploy-verify.md` | §3.4 MCP+skill health gate |
| `backend/file-map.md` | `config.js` fallback note |
| `tasks/06-25-spec-add-workspace-instant-sync-to-1-1-3-checklist/prd.md` | Delivered #20 |
| **`progress-2026-06-28.md`** | **Latest MCP+Skill snapshot (this task)** |

## Deploy note

Hot zip path: `vendor/mcp-servers/quotation-server/dist/config.js` + manifest (no AionUI required for CLI fix). UI 完整探测 `quotation:python` needs co-deploy `aionui-src` health files in same release.
