# Phase 0.5 — System-review harden (2026-07-14)

Adopted review recommendation **B** (固化执行前 blocker).

| Blocker | Action | Evidence |
|---------|--------|----------|
| task.json PS parse fail | Rewrite UTF-8 **BOM** + ASCII-safe description | `ConvertFrom-Json` OK；`meta.code_map_path_locked` set |
| CODE_MAP 落点 TBD | Lock jsonl under wanding data | `uncoded-inventory-map.md` + plan D1 |
| Agent teaches unwired `search_inventory` | Decision-table forbid | `quotation-agent.md` 仅描述库存行 |
| Write/evidence undefined | Schema + AC8 envelope | same spec |

**Next:** user `执行 task` → Phase 1 implement map lib + seed.
