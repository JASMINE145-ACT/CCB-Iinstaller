# Closure — org knowledge agent write path (2026-06-28)

## Problem

Quotation agent edited **local shadow** `D:\CCB-Wanding\vendor\wanding\data\wanding_business_knowledge.md` (deleted test1 block) instead of **VPS center** org knowledge. Other employees and `#/org-knowledge` unchanged; shadow could resync from center.

**Root cause (three gaps):**

| Gap | Detail |
|-----|--------|
| MCP | `append_business_rule` in Python `tool_dispatch` but **not** in `quotation-server/dist/index.js` ListTools |
| Agent prompt | `quotation-agent.md` only documented **Read** path to shadow md |
| Tool contract | `append_business_rule` **append-only** — delete/full edit must use `#/org-knowledge` UI |

## Fix delivered

| Change | Path |
|--------|------|
| MCP tool registered | `mcp_servers/quotation-server/dist/index.js` |
| Shadow read-only + write rules | `ccb-installer/config/agents/quotation-agent.md` |
| Registry unittest | `python/tests/test_quotation_mcp_tool_registry.py` |
| Spec | `.trellis/spec/integration/org-knowledge.md` |

## Verification

| Check | Result |
|-------|--------|
| Code review agent | **PASS** (1499ec17) |
| `python -m unittest tests.test_quotation_mcp_tool_registry` + append dispatch tests | **4/4 OK** |
| `sync-dev-wanding-vendor.ps1` | `index.js` copied to `D:\CCB-Wanding\vendor\mcp-servers\quotation-server\dist\` |
| Live vendor grep `append_business_rule` | **present** |
| `deploy-seed-agents.ps1 -ForceMd` | see deploy log below |

## Operator follow-up

1. **Delete test1 on center (if still there):** `#/org-knowledge` → `wanding_business_knowledge` → edit → save (PUT VPS).
2. **New quotation conversation** after dev restart — MCP subprocess loads updated tool list.
3. **Append rule E2E:** `append_business_rule` with `confirmed=false` → user confirms → `confirmed=true` → check `#/org-knowledge` version bump + shadow sync.

## Deferred

- Agent tool for **full-doc PUT / delete section** (UI remains canonical for delete).
- 409 conflict retry hint in agent prompt (code-review nice-to-have).

## Spec updates (2026-06-28)

Recorded in:

| Spec | Section |
|------|---------|
| `integration/org-knowledge.md` | Status, Common mistakes, MCP paths, Tests, Recorded |
| `integration/agents-unified-model.md` | L4 shadow read-only; quotation-agent write rules |
| `integration/org-knowledge-phase0-rollout.md` | Channel C append; center edit SOP |
| `integration/dev-sync-playbook.md` | §3.1 table + Recorded |
| `integration/index.md` | Decision tree row |
| `backend/config-layer.md` | Shadow vs center |
| `spec/index.md` | org-knowledge line |

**Task status:** `completed` (2026-06-28)
