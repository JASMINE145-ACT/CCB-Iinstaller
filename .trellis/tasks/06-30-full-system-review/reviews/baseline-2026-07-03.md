# P0 Baseline — 2026-07-03

> Task: `06-30-full-system-review` · Phase P0  
> Repos: `aionui-src` + `claude-code-best`  
> **No code changes** in this phase.

---

## Automated matrix A

| Check | Command | Result |
|-------|---------|--------|
| MCP health diagnosis | `bun test tests/unit/common-config/ccbMcpHealthDiagnosis.test.ts` | **PASS** 9/9 |
| Startup readiness shared | `bun test tests/unit/common-config/ccbStartupReadinessShared.test.ts` | **PASS** 4/4 |
| Skills catalog | `bun run test -- tests/unit/common-skills/fetchSkillsCatalog.test.ts` | **PASS** 2/2 (vitest via `bun run test`; raw `bun test` fails — `vi.hoisted` needs vitest) |
| Renderer Node import guard | `rg "from '@/common/config/ccbMcpHealth'" packages/desktop/src/renderer` | **PASS** 0 matches |
| MCP health CLI | `ccb-installer/scripts/test-mcp-health.ps1` | **PASS** exit 0 (51 checks) |

### Notes

- `fetchSkillsCatalog.test.ts` must run via **`bun run test`** (vitest), not bare `bun test`.
- `test-mcp-health.ps1` ran without `-Probe` / `-Session` (quick mode per plan).

---

## Manual smoke A

| Item | Status | Notes |
|------|--------|-------|
| `start-dev-full.ps1` 无 main 255 / 白屏 | **pending** | Vite `localhost:5173` down at baseline time |
| Guid 页可渲染 | **pending** | needs dev |
| Settings → 能力扩展 Skills/Tools | **pending** | 2026-07-03 white-screen fix unverified in live UI |
| Settings → 模型 / Agents | **pending** | needs dev |

**Gate:** Automated baseline **green**. Manual smoke **deferred** — start dev before P2 FE-P0-1 verification.

---

## P0 verdict

- **Automated:** ✅ proceed to P1 (Step 3 read-only audit)
- **Manual:** ⏳ complete during P2 alongside FE-P0-1 AC checks
