# Journal: 1.1.6 bootstrap exit 1 — research manifest repack

**Date:** 2026-07-04  
**Version:** CCB-Wanding 1.1.6 (repack)

## Symptom

NSIS install completes file copy but shows:

> CCB-Wanding setup completed copying files, but bootstrap returned exit code 1.

Same dialog as 1.1.2/1.1.3 (`build-deploy-verify.md` §5.1 retired-agent issue), but **different root cause** for 1.1.6.

## Root cause (1.1.6)

1. `run-wanding-bootstrap.ps1` Mode=Full runs `install-research-toolstack.ps1` → `probe-research-capabilities.ps1`.
2. Probe looked for manifest at dev-repo path `...\ccb-installer\config\research-capability-manifest.json` (via `$PSScriptRoot` parent-of-parent), which does not exist on employee machines.
3. `build-wanding.ps1` did not ship `config/research-capability-manifest.json` into staging.

**Log signature:**

```
FAIL install-research-toolstack — Missing manifest: D:\ccb-installer\config\research-capability-manifest.json
```

## Fix

| File | Change |
|------|--------|
| `build-wanding.ps1` | Copy `research-capability-manifest.json` → staging `config/` |
| `probe-research-capabilities.ps1` | Resolve manifest: `$InstallDir\config\` → script-root `config\` → dev repo |
| `install-health-manifest.json` | Assert manifest + research scripts in `platform_required_files` |
| `build-deploy-verify.md` | §5.3 |
| `packaging-backlog-1.1.6.md` | Checklist item |

## Verification

- Code-review agent: **PASS**
- `test-package-health-split.ps1`: **2/2 PASS**
- Simulated Full bootstrap on `D:\CCB-Wanding` with fix: **EXIT=0**, `bootstrap complete (marker written)`

## Repack

`build-wanding.ps1 -Version 1.1.6` → `ccb-installer/build-1.1.6-repack.log`
