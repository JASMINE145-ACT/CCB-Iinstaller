---
description: Save personal preference from the current conversation to memory/personal/
---

Guide the user to confirm what to remember, then:

1. **Personal only (1.1.7):** write to `memory/personal/workflow.md` (work habits) or `memory/personal/profile.md` (optional notes not already in Settings → 个人信息).
2. Do **not** write business quotation/customer/pricing rules here — those belong to org knowledge (`append_business_rule`) or future business memory phases.
3. Read the target file, skip duplicates, append `- [YYYY-MM-DD] concise line`.
4. Tell the user which file was updated.

If the content duplicates Settings employee profile fields (name, department, job title), ask the user to update Settings instead.
