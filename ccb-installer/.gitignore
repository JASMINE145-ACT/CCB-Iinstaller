# Downloaded Bun baseline cache (build artifact, not source)
.cache/

# Build outputs
*.exe
!resources/**/*.exe
!vendor/**/*.exe
# GitHub 100MB limit — keep local only (see root .gitignore)
vendor/mcp-servers/excel-mcp/mcp-excel.exe
