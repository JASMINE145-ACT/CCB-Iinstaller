{
  "config_generation": 3,
  "description": "Price library edit skill + agent hooks — refresh agent seeds and AionUI ACP runtime on install/upgrade",
  "reset_targets": [
    "agents",
    "aionui_runtime"
  ]
}
