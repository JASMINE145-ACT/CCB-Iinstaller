{
  "config_generation": 5,
  "description": "Personal memory Stop hook (ccb-personal-memory) + memory seed + /记住 command — refresh agents, skills, commands, CLAUDE memory rules on install/upgrade",
  "reset_targets": [
    "agents",
    "aionui_runtime"
  ]
}
