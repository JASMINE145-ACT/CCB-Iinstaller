{
  "config_generation": 6,
  "description": "Org-primary historical quotation mapping (learn-by-data Section D) + drat matcher — refresh agents, skills, commands on install/upgrade",
  "reset_targets": [
    "agents",
    "aionui_runtime"
  ]
}
