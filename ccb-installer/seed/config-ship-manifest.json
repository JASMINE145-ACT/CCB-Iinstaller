{
  "config_generation": 7,
  "description": "Supplier-directory MCP + agent seeds + WeCom docs fleet-hide — refresh agents, skills, MCP on install/upgrade",
  "reset_targets": [
    "agents",
    "aionui_runtime"
  ]
}
