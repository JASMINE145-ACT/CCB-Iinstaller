{
  "config_generation": 2,
  "description": "GUI v2 fleet reset — overwrite CLI-era agent seeds and refresh AionUI ACP runtime on install/upgrade",
  "reset_targets": [
    "agents",
    "aionui_runtime"
  ]
}
