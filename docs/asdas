还有两个独立问题叠在一起
1）对话断档（你遇到的）
Agent 选了正确工具、正确 code（8020020755）
没把 tool 结果变成表格回复
下一拍却变成「没有内容」——更像 runtime/模型续拍失败，不是 tool 报错
2）数据可能不全（即使用户看到回复也可能失望）
未走 org_api 时，bundled_seed 只有 7 列 MVP，8020020755 在完整 xlsx 里有出厂/B/大唐等多档，但工具可能只返回 1 档 price_b。这是数据层问题，和「没有内容」是两条线。